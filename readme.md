```cpp
#include <life.h>

using namespace std;

int main() {
string name = "Mohsen";
bool isProgrammer = true; 
string favs[] = {"Chess","C++","JavaScript"};
bool isAlive = true;
int age = 17;
for (int i = 0; isAlive; i++ {
    if(isUserDead()) {
      isAlive = false;
    }
    age++;
  }
  if (!isAlive) {
     return 0;
  }
}
```
